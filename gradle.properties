# ─── Gradle JVM / parallelism ────────────────────────────────────────────────
org.gradle.jvmargs=-Xmx4G
org.gradle.parallel=true

# ─── Mod metadata (shared across all MC versions) ────────────────────────────
mod_version=1.4.13
maven_group=com.lukarbonite.autopickup
archives_base_name=autopickup

# ─── Shared build-tool versions (same across all MC versions) ────────────────
loader_version=0.19.3
loom_version=1.16-SNAPSHOT
arch_loom_version=1.13-SNAPSHOT
neoforge_gradle_version=2.0.141

# ─── MC-version-specific properties live in each version's gradle.properties ─
# e.g. 1.21.9-11/fabric/gradle.properties, 26.1/neoforge/gradle.properties, etc.
